{
  "name": "alumni-portal-root",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "predeploy": "cd client && npm run build",
    "deploy": "cd client && npm run build && firebase deploy --only hosting"
  }
}
